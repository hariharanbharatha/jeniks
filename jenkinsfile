pipeline {
    agent{
        docker{ image 'node16:alpine'}
    }
            stages{
                stage('test'){
                    step{
                      sh 'node --version'
             }
         }
     }
}