pipeline {
    agent{
        docker{ image 'node16:alpine'}
    }
            stages{
                stage('test'){
                    steps{
                      sh 'node --version'
             }
         }
     }
}
